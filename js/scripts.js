function replace0(number) {
  //This function will take a number
  //convert it to a string
  var toString = number.split('');
  // determine if the number contains a zero
console.log(toString);

//and return the word beep
};



$(document).ready(function(){

$("form#form1").submit(function(event) {
var number = parseInt($("#userInput").val());
alert (number)
});
});
